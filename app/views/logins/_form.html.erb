<div class="alert alert-warning text-center">Para poder publicar un estacionamiento necesitas ingresar con una cuenta Flash Web Systems</div>
<% provide(:title, 'Sign up') %>
<h3 align="center">Estás creando una cuenta, solo tardarás unos segundos</h3>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <% if @login.errors.any? %>
      <div div id="error_explanation">
        <div class="alert alert-danger">
          The form contains <%= pluralize(@login.errors.count, "error") %>.
        </div>
        <ul>
        <% @login.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <%= form_for(@login) do |f| %>
    
      <%= f.label 'Nombre(s): ' %><br>
      <%= f.text_field :name, class: 'form-control', placeholder: 'Ingresar Nombres' %>

      <%= f.label 'Apellidos: ' %><br>
      <%= f.text_field :lastname, class: 'form-control', placeholder: 'Ingresar Apellidos' %>
      
      <%= f.label 'Email: ' %><br>
      <%= f.email_field :email, class: 'form-control', placeholder: 'Ingresar correo electronico' %>
      
      <%= f.label 'Contraseña: ' %><br>
      <%= f.password_field :password, class: 'form-control', placeholder: 'Ingresar contraseña' %>
      
      <%= f.label 'Sexo: ' %><br>
      <%= f.select :sexo, options_for_select([["Masculino", "M"], ["Femenino", "M"]]) %>
      
      <%= f.submit "Crear Cuenta", class: "btn btn-primary" %>
      
      <p align="center">¿ Ya tienes una cuenta ? <%= link_to "Ingresar", login_path %></p>
    <% end %>
  </div>
</div>

